The model of CNN is in f_model.py
Testing happens right after finishing training the model.

'python train_test.py'    to run
categories can be chosen by setting cates in train_test.py. Set it to None to load all categories.

The SVM and Naive Bayes models are from https://github.com/cahya-wirawan/ML-Collection